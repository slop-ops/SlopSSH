[package]
name = "muon-plugin-hello"
version = "0.1.0"
edition = "2024"
description = "Muon SSH plugin: example hello world plugin"

[lib]
crate-type = ["cdylib"]

[workspace]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
