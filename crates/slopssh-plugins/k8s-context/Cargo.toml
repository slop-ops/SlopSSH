[package]
name = "slopssh-plugin-k8s-context"
version = "0.1.0"
edition = "2024"
description = "SlopSSH plugin: displays current Kubernetes context in status bar"

[lib]
crate-type = ["cdylib"]

[workspace]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
